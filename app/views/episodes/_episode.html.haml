- cache episode do
  .episode-summary.large-4.columns
    = link_to episode, title: episode.title, class: 'row' do
      .preview.large-6.columns
        = image_tag episode.preview_image_url
      .info.large-6.columns
        %h5
          = episode.title
        %p
          = episode.subtitle
        - if episode.posted_at.present?
          %time{ value: episode.published_at }
            Broadcast at #{episode.posted_at}.
