require "rails_helper"

RSpec.describe <%= class_name %>Worker, type: :worker do
  let :model do
    double 'Model'
  end

  subject { <%= class_name %>Worker.new model: model }

  it "works" do
    expect(subject.perform).to be true
  end
end
