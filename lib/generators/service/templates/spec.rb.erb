require 'rails_helper'

RSpec.describe <%= class_name %>Service, type: :service do
  let :<%= local %> do
    <%= local.pluralize %> :test
  end

  let :remote do
    double '<%= remote %>'
  end

  subject do
    described_class.new local: <%= local %>
  end

  before do
    allow(<%= remote %>).to receive(:new).and_return(remote)
  end

  it 'defines attributes to copy to remote'
end
